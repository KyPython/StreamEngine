config:
  target: "ws://localhost:4000"
  phases:
    - duration: 60
      arrivalRate: 20 # 20 new connections per second for 1 minute
scenarios:
  - engine: "ws"
    flow:
      - send: { text: "hello" }
      - think: 1
